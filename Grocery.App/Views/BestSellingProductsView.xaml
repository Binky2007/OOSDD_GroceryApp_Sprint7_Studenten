<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.BestSellingProductsView"
             x:DataType="vm:BestSellingProductsViewModel"
             Title="BestSellingProductsView">

    <Shell.TitleView>
        <Grid>
            <Label Text="Best verkochte producten"
                   FontSize="Large"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <Border Stroke="#C49B33"
            StrokeThickness="4"
            Padding="10"
            Margin="10"
            HorizontalOptions="FillAndExpand">

        <!-- Grid met header + CollectionView -->
        <Grid RowSpacing="0" Padding="0" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Padding="5"  HorizontalOptions="FillAndExpand">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Text="Nummer" FontAttributes="Bold" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
                <Label Grid.Column="1" Text="Naam" FontAttributes="Bold" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
                <Label Grid.Column="2" Text="Aantal verkocht" FontAttributes="Bold" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
                <Label Grid.Column="3" Text="Aantal in voorraad" FontAttributes="Bold" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
            </Grid>

            <!-- CollectionView -->
            <CollectionView Grid.Row="1" ItemsSource="{Binding Products}" Margin="0" HorizontalOptions="FillAndExpand">

                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:BestSellingProducts">
                        <Grid Padding="5" HorizontalOptions="FillAndExpand">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="4*" />
                            </Grid.ColumnDefinitions>

                            <Label Grid.Column="0" Text="{Binding Ranking}" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="1" Text="{Binding Name}" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="2" Text="{Binding NrOfSells}" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="3" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center">
                                <Label.Text>
                                    <Binding Path="Stock" />
                                </Label.Text>
                                <Label.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding Stock}" Value="0">
                                        <Setter Property="Text" Value="niet op voorraad" />
                                        <Setter Property="TextColor" Value="Red" />
                                    </DataTrigger>
                                </Label.Triggers>
                            </Label>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Border>
</ContentPage>
